  <!-- 切换tap -->
  <view class="swiper-tab">
    <view class="swiper-tab-list">
      <view class="swiper-item  {{currentTab==0 ? 'on' : ''}}" data-current="0" bindtap="swichNav" >1122</view>
    </view>
    <view class="swiper-tab-list">
      <view class="swiper-item {{currentTab==1 ? 'on' : ''}}" data-current="1" bindtap="swichNav" >21212</view>
    </view>
  </view>
